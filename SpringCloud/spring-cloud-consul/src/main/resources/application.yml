spring:
  application:
    name: spring-cloud-consul
  cloud:
    consul:
      host: localhost
      port: 8500
      discovery:
        health-check-interval: 20s
        health-check-path: /health
        service-name: ${spring.application.name}
        instance-id: consulId
server:
  port: 8080
